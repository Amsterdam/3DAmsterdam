mergeInto(LibraryManager.library, {
    /*
    Draw a javascript alert message
     */
    ShowAlertMessage: function (alertMessage) {
        var message = UTF8ToString(message);
        alert(message);
    }
});